{
  "name": "ury-dashboard-demo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend && rm -rf dist && cp -r frontend/dist dist"
  }
}
